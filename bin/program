#!/usr/bin/env bash
echo hello world
export
echo in dir `pwd`
for each in $(find . -type d | grep -vE "/.git$" | grep -vE "/.git/" | grep -vE "/.heroku$" | grep -vE "/.heroku/")
do
  echo DIR "$each"
  ls -la "$each"
done
echo DIR "$TEMP_APP_DIR"
ls -la "$TEMP_APP_DIR"

pushd "$TEMP_APP_DIR"

wget $(dataURLFromCloudinary.py openSourceTracker.csv)

if [ ! -f openSourceTracker.csv ]
then
  echo "projectName,URL" > openSourceTracker.csv
fi

uploadDataToCloudinary.py openSourceTracker.csv openSourceTracker.csv

popd


