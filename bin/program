#!/usr/bin/env bash
echo hello world
export
echo in dir `pwd`
for each in $(find . -type d | grep -vE "/.git$")
do
  echo DIR "$each"
  ls -la "$each"
done
echo DIR "$TEMP_APP_DIR"
ls -la "$TEMP_APP_DIR"

