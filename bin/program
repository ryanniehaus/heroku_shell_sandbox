#!/usr/bin/env bash
echo hello world
export
echo in dir `pwd`
for each in $(find . -type d)
do
  echo DIR "$each"
  ls -la "$each"
done

if [ -f new_file.txt ]
then
  echo FILE ALREADY EXISTS
else
  > new_file.txt
fi

